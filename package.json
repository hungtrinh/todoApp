{
  "name": "todoapp",
  "version": "1.0.0",
  "description": "tdd without mocking, without external lib support mock & stub",
  "main": "index.js",
  "scripts": {
    "migrate": "npm run env:test knex migrate:latest",
    "test": "tape app/**/*.unittest.js | tap-spec",
    "test:db": "npm run env:test tape test/*.js tape app/**/*.dbtest.js | tap-spec",
    "test:watch": "watch 'clear && npm run -s test' app -du",
    "test:cover": "NODE_ENV=test PORT=0 istanbul cover tape app/**/*.{unittest,dbtest}.js",
    "fix": "standard --fix app/**",
    "start": "NODE_ENV=development PORT=3000 pm2-dev index.js",
    "pull": "git pull -r && npm i && npm run test",
    "env:test": "NODE_ENV=test PORT=0 DB_UERNAME=todo_test DB_PASSWORD=todo_test DB_NAME=todo_test DB_HOST=localhost DB_PORT=3306 DB_CONNECTION_LIMIT=1 DEBUG=test"
  },
  "keywords": [
    "todo",
    "koa"
  ],
  "author": "hungtd",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "4.0.0",
    "istanbul": "0.4.5",
    "standard": "10.0.3",
    "supertest": "3.0.0",
    "tap-spec": "4.1.1",
    "tape": "4.8.0",
    "tape-async": "2.3.0",
    "watch": "1.0.2"
  },
  "dependencies": {
    "joi": "13.1.1",
    "knex": "0.14.2",
    "koa": "2.4.1",
    "koa-bodyparser": "4.2.0",
    "koa-router": "7.3.0",
    "mysql": "2.15.0",
    "pm2": "2.9.3"
  },
  "repository": "git@github.com:hungtrinh/todoApp.git"
}
